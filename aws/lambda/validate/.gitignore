# Generated by build.sh/copy-src.sh -- a raw copy of the repo's src/ and
# index.html, plus installed deps and the packaged zip. Never edit these in
# place; edit the real ../../../src or ../../../index.html and re-run
# build.sh (or copy-src.sh for the SAM path).
/src/
/index.html
/node_modules/
/package-lock.json
/function.zip
